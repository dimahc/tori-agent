name: Auto-assign PR

on:
  pull_request_target:
    types: [opened]

permissions:
  pull-requests: write

jobs:
  auto-assign:
    runs-on: ubuntu-latest
    steps:
      - name: Assign PR to author
        uses: peter-evans/auto-assign@v5
        with:
          assignees: ${{ github.actor }}
